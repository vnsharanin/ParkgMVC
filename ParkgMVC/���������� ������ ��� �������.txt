в Контроллере

        public ActionResult ZonesLevelsPlaces()
        {
            ViewData["Fu"] = new SelectList(recordsVISITS.ParkingZone(), "", "Parking_zone");
            return View(recordsVISITS.ParkingZone());
        }


во View

    <%= Html.DropDownList("fsselect", (SelectList)ViewData["Fu"], "Выберите") %>


СПОСОБ 2:
в RecordsVISITS:

        public List<int> Nu()
        {
            List<int> Lis = new List<int>();
            for (int i = 1; i < 5; i++)
            {
                Lis.Add(i);
            }
            return Lis;
        }

в контроллере:

        public ActionResult ZonesLevelsPlaces()
        {
            ViewData["Fu"] = new SelectList(recordsVISITS.Nu() , "");
            return View(recordsVISITS.ParkingZone());
        }

во view:

        <%= Html.DropDownList("fsselect", (SelectList)ViewData["Fu"], "Выберите") %>


!!!первый параметр Html.DropDownList("fsselect"...) это имя поля, он должен соответствовать названию переменной.
public ActionResult Action(string fsselect)
И передан он будет в эту переменную.